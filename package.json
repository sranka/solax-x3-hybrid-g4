{
  "name": "solax-x3-hybrid-g4",
  "version": "1.1.0",
  "description": "",
  "main": "scripts/dev-server.js",
  "scripts": {
    "start": "sh scripts/stamp-build.sh && node scripts/dev-server.js",
    "dev:insecure": "open -a 'Google Chrome' --args --disable-web-security --user-data-dir=/tmp/chrome-dev-solax http://localhost:8080 & python3 -m http.server 8080 -d web",
    "build": "pnpm run build:android",
    "build:android": "sh scripts/stamp-build.sh && npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:ios": "sh scripts/stamp-build.sh && npx cap sync ios && cd ios/App && xcodebuild -project App.xcodeproj -scheme App -configuration Debug -destination 'generic/platform=iOS Simulator' build CODE_SIGN_IDENTITY=- CODE_SIGNING_ALLOWED=NO",
    "release": "sh scripts/release.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.2.0",
    "@capacitor/cli": "^8.2.0",
    "@capacitor/core": "^8.2.0",
    "@capacitor/ios": "^8.2.0"
  }
}
